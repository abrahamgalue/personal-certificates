---
import Layout from '@/layouts/Layout.astro'
---

<Layout title="500 - Error del servidor">
  <main class="error-wrapper">
    <div class="content">
      <span class="code" aria-hidden="true">500</span>
      <h1 class="title">Error interno</h1>
      <p class="desc">
        Ocurrió un problema inesperado. Intenta de nuevo más tarde o vuelve al
        inicio.
      </p>
      <a href="/" class="back-btn" aria-label="Volver al inicio">
        Volver al inicio
      </a>
    </div>
  </main>
</Layout>

<style>
  .error-wrapper {
    margin: 0 auto;
    max-width: 800px;
    padding: 6rem 1rem 4rem;
    text-align: center;
  }

  .content {
    align-items: center;
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .code {
    background: linear-gradient(135deg, #f87171, #ef4444, #dc2626);
    font-size: 5rem;
    font-weight: 800;
    line-height: 1;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .title {
    font-size: 1.75rem;
    font-weight: 700;
    margin: 0;
    background: linear-gradient(135deg, #e2e8f0, #94a3b8);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .desc {
    color: #64748b;
    font-size: 1rem;
    max-width: 520px;
    margin: 0 0 0.5rem;
  }

  .back-btn {
    background: linear-gradient(135deg, #3b82f6, #1d4ed8);
    border: 1px solid #2563eb;
    border-radius: 10px;
    color: #fff;
    font-size: 0.9rem;
    font-weight: 600;
    padding: 0.75rem 1.5rem;
    text-decoration: none;
    transition: all 0.25s ease;
  }

  .back-btn:hover,
  .back-btn:focus-visible {
    background: linear-gradient(135deg, #2563eb, #1d4ed8);
    border-color: #3b82f6;
    box-shadow: 0 12px 30px rgba(59, 130, 246, 0.25);
    outline: none;
  }

  @media (width <= 600px) {
    .code {
      font-size: 3.5rem;
    }
    .title {
      font-size: 1.4rem;
    }
  }
</style>
